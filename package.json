{
  "name": "imhooked-fishing-charters",
  "version": "1.0.0",
  "private": true,
  "description": "Static reproduction of imhookedfishingcharters.com.au, built for Cloudflare",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "serve": "node scripts/serve.mjs",
    "dev": "node build.mjs && node scripts/serve.mjs",
    "deploy": "npm run build && npx --yes wrangler@latest deploy",
    "preview": "npm run build && npx --yes wrangler@latest versions upload"
  },
  "devDependencies": {
    "@fontsource-variable/nunito-sans": "^5.3.0"
  }
}
